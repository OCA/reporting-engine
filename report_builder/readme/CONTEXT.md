This module is inspired by mis-builder and shares its goal of grid-based KPI reporting. 
However, it takes a different architectural approach: rather than being tightly coupled to accounting, it is designed to be pluggable on any Odoo model through small glue modules.

A migration path from mis-builder is planned but not yet available. Both modules can coexist in the meantime.
